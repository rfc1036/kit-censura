# Esempi / Template operativi

- `crontab`: esempio di scheduling per `censorship-cron`.
- `kit-censura.logrotate`: template di logrotate; copiare in `/etc/logrotate.d/` dopo averlo adattato.

Questi file non sono necessari a runtime: servono come reference e vanno personalizzati per il proprio ambiente.
